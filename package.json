{
  "name": "anum-parser-lab",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Экспериментальная лаборатория сериализации и десериализации ачисел МТС",
  "scripts": {
    "mts:prepare": "node scripts/materialize-mts-core.mjs",
    "pretest": "npm run mts:prepare",
    "test": "node --test tests/*.test.mjs",
    "precheck": "npm run mts:prepare",
    "check": "node --check src/model.js && node --check src/formats.js && node --check src/carrier.js && node --check src/deserializers.js && node --check src/serializers.js && node --check src/visual-model.js && node --check src/cytoscape-adapter.js && node --check src/visualizer.js && node --check src/app.js"
  },
  "engines": {
    "node": ">=24"
  }
}
